
  <section class="search-section">
    <div class="search-background">
      <div class="search-bar">
        <input type="num" placeholder="Search by zip code"/>
        <input type="submit" value="Search" />
      </div>
    </div>
  </section>

<div class="container">
  <% for listing in @listings %>
  <div class="listing">
    <h2><%= listing.title %></h2>
    <p class="body">
      <%= listing.body %>
    </p>
    <p class="zip">
      <%= listing.zip %>
    </p>
    <% for photo in listing.photos %>
      <%= image_tag photo.picture.url(:medium) %>
    <% end %>
  </div>
  <% end %>
</div>

<script>
  var listing = document.querySelector(".zip");
  var result = parseInt(listing.textContent);
  var search = document.querySelector('input[type="num"]');

  search.addEventListener("enter", searchIt);

  function searchIt () {
    // e.preventDefault();

    search = parseInt(search.value);



    if (result == search) {
      var match = listing.parentElement
      return "yes!"
      debugger
    };

  }

</script>